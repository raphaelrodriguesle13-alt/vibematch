name: VibeMatch Phone Routes Formatter (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retirement
        run: |
          echo 'This one-shot formatter workflow is retired.'
          echo 'Canonical formatting is enforced by the main VibeMatch CI.'
          echo 'No repository write permission is granted and no files are modified.'
