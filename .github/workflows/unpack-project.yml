name: Unpack VibeMatch project (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo 'This workflow is retired.'
          echo 'The VibeMatch sources are already unpacked under vibematch/.'
          echo 'No repository write permission is granted and no files are modified.'
