# Infraestrutura — NÃO IMPLEMENTADA NESTA ETAPA

IaC para GCP (Cloud Run, Cloud SQL, Redis gerenciado, Secret Manager, GCS)
pertence às etapas posteriores. Ambiente local usa `docker-compose.yml` na raiz.

Nenhuma credencial, service account JSON ou configuração de produção neste diretório.
